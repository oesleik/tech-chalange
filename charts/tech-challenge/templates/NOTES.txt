1. `kubectl get pods -n {{ .Release.Namespace }}`
2. Acesso local (Minikube ou MiniStack): `make k8s-url` / `make aws-local-url` — port-forward em `http://localhost:30080`
3. phpMyAdmin (só Minikube): `make k8s-phpmyadmin-url` — `http://localhost:30081`
